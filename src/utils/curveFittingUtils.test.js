import { describe, it, expect } from 'vitest';
import * as curveFittingUtils from './curveFittingUtils.js';

describe('curveFittingUtils', () => {
    it('should export functions', () => {
        expect(curveFittingUtils).toBeDefined();
    });
});
